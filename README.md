# Fundraise Up fullstack test task
Before run install npm packages:
```shell script
npm run install:all
yarn install:all
```

To run the app locally:
```shell script
// client
npm run client:dev
yarn client:dev

// server
npm run server:dev
yarn server:dev
```
